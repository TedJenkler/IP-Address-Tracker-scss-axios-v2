import { useEffect, useState } from "react";

const useSearch = () => {
    useEffect(() => {
        const fetchData = async () => {
            try {

            } catch (error) {
                
            }
        }
    }, [])
}

export default useSearch;